/**
 * Zadanie 10.
 * Napisz program, który pokazuje, które atrybuty procesu macierzystego są dziedziczone
 * przez proces potomny uruchomiony za pomocą funkcji fork(), które zaś otrzymują nową wartość.
 */
#include <stdio.h>

int main(int argc, char * argv[]) {
    return 0;
}
