/**
 * Zadanie 12.
 * Napisz program, który wyświetla identyfikator procesu (PID) i nazwę
 * związanego z nim polecenia dla wszystkich procesów uruchomionych przez użytkownika
 * podanego w wierszu wywołania programu.
 * Wskazówka: informacje te można uzyskać przeglądając katalog /proc z plików
 * (interesują nas katalogi, których właścicielem jest dany użytkownik) i pliki /proc/PID/status.
 */
#include <stdio.h>

int main(int argc, char * argv[]) {
    return 0;
}
